extend layout
block content
  .container(data-module="dashboard" class="center full-height five-sixths")
    h3 #{description}
    if (!user)
		a(href="/login") Login
		br
		a(href="/register") Register
	if (user)
		p You are currently logged in as #{user.first_name}
		a(href="/logout") Logout