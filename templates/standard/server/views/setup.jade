extend layout
block content
	.container(data-module="setup" class="center full-height five-sixths")
		h3 #{description}

		form(id="signup-form" class="center text-centered two-row one-fourth")
			input(name="submitted" type="text")
			button(id="signup-btn") Signup!